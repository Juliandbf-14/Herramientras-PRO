
using System;

namespace Prueba
{
    class Program
    {
        // pida al usuario una temperatura en grados Celsius y muestra su equivalente en grados Fahrenheit usando la fórmula: F = C * 9/5 + 32.

        static void Main(string[] args)
        {
            int numero = 0;
            Console.WriteLine("Ingrese el número para verificar si es primo: ");
            numero = Int32.Parse(Console.ReadLine());

            if (numero / numero)
            {
                
            }
        }
    }
}
